import InternalWrapper from "../ui/InternalWrapper";

const AboutUs: React.FC = () => {
  return (
    <section id="about-us" className="flex-1 flex items-center justify-center bg-intro-background bg-no-repeat bg-cover">
        <InternalWrapper>
            <section className="w-full h-screen flex items-center justify-center px-4 py-8">
               

            </section>
        </InternalWrapper>
    </section>
  );
}

export default AboutUs;
